import React from 'react';
import {connect} from "react-redux";

class GuitarsContainer extends React.Component {

    render() {
        return (
            <></>
        );
    }
}

const mstp = state => ({

})


export default connect(mstp, {})(GuitarsContainer);